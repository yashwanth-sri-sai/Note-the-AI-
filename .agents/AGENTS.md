# Agent Rules

- **Git Commits**: Always automatically run `git push` immediately after making a `git commit`, unless explicitly told otherwise. Never leave commits sitting locally if the goal is to upload or update code.
